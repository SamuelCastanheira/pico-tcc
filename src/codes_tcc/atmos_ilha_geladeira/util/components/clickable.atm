task Clickable(id, rect)
{
    loop {
        loop e on :mouse.button.dn {
            val m = pico.get.mouse('%')
            if pico.vs.pos.rect(m, rect) {
                set pub = true
            }
        }
    }

}

Clickable