task XCollidable (me, others) {
    loop on :clock {
        loop _,e in others {
            val col = pico.vs.pos.rect(me.pub.rect, e.pub.rect)
            if col {
                emit @(me) :collided [id=e.pub.id]
            }
        }
    }
}

XCollidable
