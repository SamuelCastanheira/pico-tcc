func Clickable(id, rect)
{
    loop {
        every e in :mouse.button.dn {
            val m = pico.get.mouse('%')
            if pico.vs.pos_rect(m, rect) {
                set pub = true
                ;;await :frame
                set pub = false
            }
        }
    }

}

Clickable
