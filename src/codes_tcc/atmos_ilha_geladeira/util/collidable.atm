task Collidable(id, rect, other)
{
    var collided = false
    loop
    {
        await :clock
        loop _,e in other{
            if (e && e.pub.rect)
            {
                set col = pico.vs.rect.rect(e.pub.rect, rect)
                if col => set collided = e.pub.id
            }
        }
        if collided => set pub = collided
    }
}

Collidable
